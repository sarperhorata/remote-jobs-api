# Security audit settings
audit-level=moderate
fund=false

# Performance settings - Updated for stability
prefer-offline=false
cache-max=3600000

# Registry settings
registry=https://registry.npmjs.org/

# Engine settings for stability
engine-strict=false
legacy-peer-deps=true 