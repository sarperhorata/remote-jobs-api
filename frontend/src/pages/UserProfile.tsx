import React from 'react';

const UserProfile: React.FC = () => {
  return (
    <div>
      <h1>User Profile</h1>
      {/* TODO: Implement user details, resume management, application history etc. */}
      <p>User dashboard and profile information will be here.</p>
    </div>
  );
};

export default UserProfile; 